
<div class="col-lg-3 col-md-4 col-xs-12 thumb img">
<%= link_to image_tag(photo.image_url(:thumb).to_s), gallery_photo_path(photo.gallery_id, photo) %>
<%= link_to "Edit", edit_gallery_photo_path(photo.gallery_id, photo) if current_user %>
<br />
<%= link_to 'Delete', gallery_photo_path(photo.gallery_id, photo), method: :delete, data: { confirm: "Are you sure?" } if current_user %>
</div>